server:
  port: 5000
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mi7r/spring-cloud-pkp-excercise
          search-paths: configuration-files
          try-master-branch: false
          skip-ssl-validation: true
